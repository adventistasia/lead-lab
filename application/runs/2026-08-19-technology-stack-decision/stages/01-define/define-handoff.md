# Define Handoff: Lead Lab Technology Decision
**AI-generated by:** Alson (A.I Agent)

The cycle is ready for Design. The recommended baseline is Laravel with Inertia React, TypeScript, shadcn/ui, PHP, MySQL or MariaDB, private local storage, database-backed search, and the existing supported web server. The environment is not yet evidenced, so the recommendation is conditional rather than a production commitment.

## Design Inputs

| Item | Required treatment |
|---|---|
| Product boundary | Use the approved Option B scope and exclusions |
| Schedule | Prioritize the vertical slice before broader features |
| Environment | Record actual runtime, database, storage, staging, deployment, HTTPS, backup, and ownership facts |
| Authentication | Use application-managed session access as the MVP baseline; requester direction defers Microsoft 365 SSO, while participant-data rules and canonical register reconciliation remain required |
| Security | Enforce server-side authorization and private file access |
| Video | Use YouTube embedding for the MVP and record the residual URL-sharing risk |
| Deployment | Use the existing team-supported server pattern; avoid microservices and Docker by default |

## Required Design Result

Produce a solution design, environment gate, and design handoff that map the vertical slice to source components and acceptance checks.
