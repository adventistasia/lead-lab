# Build Handoff: Admin Guidelines V0.1
**AI-generated by:** Alson (A.I Agent)

**Result:** The local application now contains an administrator-only Admin Guidelines V0.1 page, sidebar entry, and one light-theme real-portal GIF with a visible mouse pointer for each How To workflow. The implementation is ready for review, not a production release.

## Changed Source

| Area | Paths | Change |
|---|---|---|
| Protected route | `application/source/routes/web.php` | Added `admin.guidelines.index` at `/admin/guidelines` inside the existing authenticated, active, and administrator middleware group. |
| Administrator navigation | `application/source/resources/js/components/app-sidebar.tsx` | Added the `Admin Guidelines V0.1` menu item with a checklist icon only to the administrator navigation list. |
| Guidelines page | `application/source/resources/js/pages/admin/guidelines/index.tsx` | Added the read-only page with the required `Description`, `Responsibilities`, and `How To's` sections, workflow links, real-portal recordings, IT boundary, responsive layout classes, and AI attribution. |
| Workflow media | `application/source/public/admin-guidelines-members.gif`; `application/source/public/admin-guidelines-classroom.gif`; `application/source/public/admin-guidelines-calendar.gif`; `application/source/public/admin-guidelines-q-and-a.gif` | Added one responsive, full-size-linkable light-theme GIF with a visible mouse pointer per How To workflow. The Members GIF uses filtered synthetic account frames. |
| Access coverage | `application/source/tests/Feature/LeadLabAccessTest.php` | Added administrator, participant, and guest route tests. |
| Current documentation | `docs/how-to/publish-a-learning-session.md`; `docs/reference/portal-reference.md` | Updated the administrator entry point to Classroom, renamed Category to Season, documented the current batch upload boundary of up to 10 files at 10 MB each, added role and Q&A behavior, and separated technical operations from portal administration. |

## Runtime Boundary

The page is read-only. It adds no database field, controller, API, external service, storage path, authorization model, infrastructure requirement, or technical operator capability. Existing server-side authorization remains the security boundary; the sidebar condition is only a navigation control.

Generated Wayfinder files under `application/source/resources/js/routes/` remain ignored and were not added to the maintained source changes.

## Verification

| Check | Result |
|---|---|
| `php artisan test tests/Feature/LeadLabAccessTest.php --filter=guidelines` | Pass: 3 tests, 11 assertions |
| `composer run test` | Pass: Pint, PHPStan with 0 errors, 147 tests, 1,184 assertions |
| `npm run format:check` | Pass |
| `npm run types:check` | Pass |
| `npm run lint:check` | Pass |
| `npm run build` | Pass |
| `php artisan route:list --json` assertion | Pass: `GET|HEAD admin/guidelines admin.guidelines.index` with `web`, `auth`, `active`, and `admin` middleware |
| `git diff --check` | Pass |
| Local authenticated workflow capture | Pass: four light-theme real-portal frame sequences captured at 1280x900 with seeded synthetic data, visible pointers, and maintained GIF assets |

## Review Handoff

1. Confirm the page shows the three required sections, four workflow recordings, and the links open Members, Classroom, and Calendar.
2. Confirm a participant does not see the item and receives `403` for direct access.
3. Repeat the local browser review on staging at desktop, tablet, and mobile widths, including keyboard focus and light and dark themes, after the deployment path is supplied.

No staging URL, access method, or deployment command was supplied, so no staging change was made.
