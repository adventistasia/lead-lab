# LEADHub Email Branding Delivery Run
**AI-generated by:** Alson (A.I Agent)

**Result:** CHG-49 (Correct email-facing branding and stale deployment defaults) is implemented locally. Email-facing output now uses exact `LEADHub`; the portal remains `Lead Hub`, the sender address remains unchanged, and private deployment plus live SMTP verification remain open.

| Field | Value |
|---|---|
| Project | Lead Hub portal (`lead-lab`) |
| Run slug | `2026-09-11-email-branding` |
| Run path | `/Users/agnojf/Documents/lead-lab/application/runs/2026-09-11-email-branding/` |
| Approved change | CHG-49 (Correct email-facing branding and stale deployment defaults) |
| Approved decision | D-72 (Should email messages use exact `LEADHub` branding while the portal remains `Lead Hub`?) |
| Active action | ACT-69 (Implement and verify LEADHub email branding across notification output and runtime configuration) |
| Start date | 2026-09-11 |
| Local status | Implemented; focused email tests pass |
| Deployment status | Handoff prepared; private deployment `.env` is not available in this workspace |
| Live delivery status | Not verified; live SMTP and staging access are unavailable |

## Scope

- Use exact `LEADHub` for email sender display, headers, footers, fallback salutations, subjects, and message bodies.
- Keep portal `APP_NAME=Lead Hub` and browser-facing branding unchanged.
- Preserve `no-reply@leadlab.test`, repository and package identifiers, technical prefixes, routes, storage paths, and production scope.
- Update local and deployment examples, notification templates, operations guidance, tests, and PM Control records.
- Do not rewrite historical email evidence or previously delivered messages.

## Stage State

| Stage | State | Evidence |
|---|---|---|
| Define and design | Complete | D-72 (Should email messages use exact `LEADHub` branding while the portal remains `Lead Hub`?) and CHG-49 (Correct email-facing branding and stale deployment defaults) |
| Build | Complete locally | `stages/03-build/build-handoff.md` |
| Measure | Conditional local pass | `stages/04-measure/audit-findings.md` |
| Learn and handoff | Complete for local-to-staging handoff | `stages/05-learn/what-now.md` |

## Validation Summary

- Focused email coverage passed 35 tests with 178 assertions across verification, password reset, registration alert, and calendar reminder paths.
- Rendered email assertions confirm `LEADHub` is present and `Lead Lab` is absent from the tested messages.
- `composer run test`: Pint passed, PHPStan passed with 0 errors, and 159 tests with 1,322 assertions passed.
- `npm run format:check`, `npm run types:check`, `npm run lint:check`, and `npm run build:ssr` passed. The existing non-blocking SSR sourcemap warning remains.
- `git diff --check` passed.
- `MAIL_BRAND_NAME=LEADHub` is configured separately from portal `APP_NAME=Lead Hub`.
- Deployment Compose defaults and handoff documentation use `LEADHub` for email display branding.
- The local source `.env` was corrected but is ignored and was not committed.
- Full project validation and diff review are recorded in `stages/04-measure/audit-findings.md`.

## Evidence Boundary

The private deployment `.env`, staging URL, live SMTP provider, queue worker, scheduler, provider delivery logs, and participant-use evidence were not available in this workspace. Local tests and rendered notification output do not certify staging or production delivery.

## Rollback

Revert the CHG-49 (Correct email-facing branding and stale deployment defaults) source, template, deployment-example, operations, test, and control-record changes. Restore `MAIL_BRAND_NAME` and email template output only if the PM authorizes rollback; keep the portal and sender address boundaries explicit.
