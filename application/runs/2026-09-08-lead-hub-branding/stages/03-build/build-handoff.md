# Build Handoff: Lead Hub Branding
**AI-generated by:** Alson (A.I Agent)

**Result:** The local application now uses the approved `Lead Hub` portal-facing identity with the supplied final logo and browser favicon. The implementation is ready for controlled staging handoff, not production release.

## Changed Source

| Area | Paths | Change |
|---|---|---|
| Visible logo and favicon | `application/source/public/lead-hub-logo.png`; `application/source/public/favicon.png`; `application/source/resources/views/app.blade.php` | Added the supplied 2048 x 878 primary logo and 512 x 512 browser icon; removed old favicon links; retained `apple-touch-icon.png`. |
| Logo components and shell | `application/source/resources/js/components/app-logo-image.tsx`; `application/source/resources/js/components/app-logo.tsx`; `application/source/resources/js/components/app-header.tsx`; `application/source/resources/js/layouts/auth/auth-card-layout.tsx`; `application/source/resources/js/layouts/auth/auth-simple-layout.tsx`; `application/source/resources/js/layouts/auth/auth-split-layout.tsx` | Updated visible logo alt text, display labels, image treatment, desktop sidebar image width to `208px`, login image width to `320px`, and scoped visual alignment adjustments. |
| Portal pages and messages | `application/source/resources/js/app.tsx`; `application/source/resources/js/pages/welcome.tsx`; `application/source/resources/js/pages/dashboard.tsx`; `application/source/resources/js/pages/calendar.tsx`; `application/source/resources/js/pages/sessions/show.tsx`; `application/source/resources/js/pages/admin/members/index.tsx`; `application/source/resources/js/pages/auth/register.tsx`; `application/source/resources/js/pages/auth/registration-pending.tsx`; `application/source/resources/js/components/nav-main.tsx`; `application/source/app/Http/Middleware/EnsureActiveUser.php`; `application/source/app/Http/Controllers/AdminLearningSessionController.php`; `application/source/app/Notifications/` | Updated portal-facing titles, labels, access messages, support wording, notification copy, and homepage image width to `320px` with a narrow-screen cap while retaining the `LeadLab Growth Series` program name. |
| Display configuration | `application/source/.env.example`; `application/source/.env`; `application/source/config/app.php`; `application/source/config/cache.php`; `application/source/config/database.php`; `application/source/config/logging.php`; `application/source/config/mail.php`; `application/source/config/session.php`; `application/source/composer.json` | Set the display name to `Lead Hub`, pinned existing runtime prefixes, and updated the project description without changing package or repository identity. |
| Seed and tests | `application/source/database/seeders/DatabaseSeeder.php`; `application/source/tests/Feature/Auth/PasswordResetTest.php`; `application/source/tests/Feature/Auth/RegistrationTest.php`; `application/source/tests/Feature/CalendarReminderTest.php`; `application/source/tests/Feature/CalendarTest.php`; `application/source/tests/Feature/LeadLabAccessTest.php` | Updated display fixtures and assertions; preserved technical test domains, class names, and access method names. |
| Active documentation and control | `CONCEPTS.md`; `CONTEXT.md`; `application/CONTEXT.md`; `application/deploy/README.md`; `application/environment/`; `application/ops/`; `artifacts/lead-lab-web-app-{scope-and-requirements,backlog,delivery-plan}.md`; `docs/`; `workspace/`; `pm-control/` | Updated active portal-facing references and recorded CHG-42 (Rename the portal-facing brand from Lead Lab and LeadLab to Lead Hub), D-67 (Should the approved Lead Hub portal-only rename be implemented now with the supplied final logo and browser favicon?), and D-57 (Should favicon and app-touch icons also be updated to use a derived square mark?). |

## Runtime Boundary

The following non-secret values are ready for staging handoff to Dennis Arquillano:

```dotenv
APP_NAME="Lead Hub"
VITE_APP_NAME="${APP_NAME}"
SESSION_COOKIE=lead-lab-session
CACHE_PREFIX=lead-lab-cache-
REDIS_PREFIX=lead-lab-database-
```

Keep `MAIL_FROM_ADDRESS="no-reply@leadlab.test"`, technical domains, URLs, package and repository identifiers, image and deployment names, storage paths, and production configuration unchanged. Do not copy secrets into this run.

## Verification

| Check | Result |
|---|---|
| `composer run test` | Pass: Pint, PHPStan with 0 errors, 144 tests, 1,173 assertions |
| `npm run format:check` | Pass |
| `npm run types:check` | Pass |
| `npm run lint:check` | Pass |
| `npm run build:ssr` | Pass: client and SSR builds; `fontaine` installed; existing sourcemap notice is non-blocking |
| `php artisan migrate:status --no-ansi` | Pass: all migrations are `Ran` locally |
| `file` and `sips` asset checks | Pass: primary logo 2048 x 878 RGBA; browser icon 512 x 512 RGBA |
| Local HTTP smoke checks | Pass: `/`, `/login`, `/lead-hub-logo.png`, and `/favicon.png` returned 200 |
| Local SSR title and icon shell | Pass: `/` returns `Lead Hub - Lead Hub`; `/login` returns `Log in - Lead Hub`; the shell references the new favicon, unchanged Apple touch icon, and primary logo |
| Requested logo image sizes | Pass by source and local SSR: desktop sidebar image width `208px`; login image width `320px`; homepage image width `320px` with `max-width: calc(100vw - 3rem)` |
| Targeted logo alignment review | Pass by requester review: the login logo is visually centered with the surrounding content and the expanded-sidebar logo is visually left-aligned with navigation content; broader authenticated responsive, light/dark, and staging review remains open |
| `git diff --check` | Pass |

## Staging Handoff

1. Dennis Arquillano should apply the non-secret display values in the staging environment and deploy the two new PNG assets.
2. Rebuild the frontend and clear application configuration or view caches after applying environment values.
3. Restart long-running application and frontend development processes so they do not retain the former `VITE_APP_NAME`.
4. Verify staging migration status before applying any pending migration; no branding migration is required.
5. Perform authenticated browser checks across public, authentication, header, sidebar, collapsed-sidebar, desktop, tablet, mobile, light, and dark states.

No staging URL, access method, or deployment command was supplied, so no staging change was made.

## Rollback

Revert the changed source and restore the prior public assets. Runtime prefix values must remain aligned with the deployed release during rollback to avoid session or cache-key mismatches.
