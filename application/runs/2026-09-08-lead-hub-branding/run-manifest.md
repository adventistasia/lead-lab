# Lead Hub Branding Delivery Run
**AI-generated by:** Alson (A.I Agent)

**Result:** CHG-42 (Rename the portal-facing brand from Lead Lab and LeadLab to Lead Hub) is implemented and locally verified within the approved local and staging-handoff boundary. Production remains unchanged.

| Field | Value |
|---|---|
| Project | Lead Hub portal (`lead-lab`) |
| Run slug | `2026-09-08-lead-hub-branding` |
| Run path | `/Users/agnojf/Documents/lead-lab/application/runs/2026-09-08-lead-hub-branding/` |
| Approved change | CHG-42 (Rename the portal-facing brand from Lead Lab and LeadLab to Lead Hub) |
| Approved decision | D-67 (Should the approved Lead Hub portal-only rename be implemented now with the supplied final logo and browser favicon?) |
| Browser icon decision | D-57 (Should favicon and app-touch icons also be updated to use a derived square mark?) -> browser favicon only; Apple touch icon unchanged |
| Related actions | ACT-62 (Prepare and implement the portal-only Lead Hub rename only after separate PM implementation authorization); ACT-51 (Complete visual verification of the approved supplied LeadLab logo treatment) |
| Start date | 2026-09-08 |
| Local status | Implemented; automated, HTTP, and requester-reviewed logo alignment checks pass |
| Staging status | Handoff prepared; staging URL and access method are not available in this workspace |
| Production status | Unchanged; no production deployment or configuration change authorized |

## Scope

- Use the supplied primary logo on public, authentication, header, sidebar, and collapsed-sidebar surfaces.
- Use the supplied square icon for the browser favicon only.
- Keep `apple-touch-icon.png` unchanged.
- Update active portal-facing copy, titles, access and support wording, notifications, display defaults, package description, current-facing documentation, and related tests.
- Preserve `LeadLab Growth Series`, `LeadLab Director`, `canAccessLeadLab()`, `LEAD_LAB_*` technical behavior, URLs, domains, repository and package identifiers, image and deployment names, storage paths, sender identity and address, and production configuration.

## Source Assets

| Asset | Source | Repository destination | Validation |
|---|---|---|---|
| Primary logo | `/Users/agnojf/Documents/alson-workspace/workspaces/logo-creator/runs/lead-hub-20260907-001/final/primary-2048.png` | `application/source/public/lead-hub-logo.png` | 2048 x 878 RGBA PNG |
| Browser icon | `/Users/agnojf/Documents/alson-workspace/workspaces/logo-creator/runs/lead-hub-20260907-001/final/icon-512.png` | `application/source/public/favicon.png` | 512 x 512 RGBA PNG |

## Stage State

| Stage | State | Evidence |
|---|---|---|
| Define and design | Complete | D-67 (Should the approved Lead Hub portal-only rename be implemented now with the supplied final logo and browser favicon?) and D-57 (Should favicon and app-touch icons also be updated to use a derived square mark?) |
| Build | Complete locally | `stages/03-build/build-handoff.md` |
| Measure | Conditional local pass | `stages/04-measure/audit-findings.md` |
| Learn and handoff | Complete for local-to-staging handoff | `stages/05-learn/what-now.md` |

## Validation Summary

- `composer run test`: Pint passed, PHPStan passed with 0 errors, and 144 tests with 1,173 assertions passed.
- `npm run format:check`, `npm run types:check`, and `npm run lint:check`: passed.
- `npm run build:ssr`: client and SSR builds passed; the optional `fontaine` peer is installed. One existing SSR sourcemap notice remains non-blocking.
- `php artisan migrate:status --no-ansi`: all migrations, including `2026_09_08_000000_add_timezone_to_users_table`, are `Ran` locally.
- `git diff --check`: passed.
- Local `/` and `/login` returned HTTP 200. `/lead-hub-logo.png` and `/favicon.png` returned HTTP 200 with `image/png` content.
- The HTML shell references `favicon.png`, `apple-touch-icon.png`, and `lead-hub-logo.png`; old `favicon.ico` and `favicon.svg` links are absent.
- After restarting Vite with the project-default host, local SSR returned `<title data-inertia="">Lead Hub - Lead Hub</title>` for `/` and `<title data-inertia="">Log in - Lead Hub</title>` for `/login`.
- Current image widths are `208px` in the desktop sidebar, `320px` on the login page, and `320px` on the homepage, with the homepage capped to the available narrow-screen width. The login image has a scoped visual-centering adjustment, and the expanded sidebar image has a scoped visual left-alignment adjustment.
- The requester checked the login and expanded-sidebar output and confirmed the results passed. This targeted review does not replace the broader authenticated responsive, light/dark, or staging acceptance gates.

## Known Evidence Boundary

The source `resources/js/app.tsx`, production SSR bundle, and restarted local Vite process use `Lead Hub`. The earlier Vite process was started on 2026-08-28 with the former `VITE_APP_NAME`; it was replaced so the current local SSR title and logo preload now use the current value.

Interactive authenticated, responsive, light/dark, and staging review is not recorded because no browser automation or staging access is available in this workspace.

## Rollback

Revert the CHG-42 source, asset, documentation, and control-record changes. Restore the prior logo and favicon only if the PM authorizes rollback. No database rollback is required for the branding change.
