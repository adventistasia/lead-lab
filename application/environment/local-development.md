# Lead Lab Local Development Environment
**AI-generated by:** Alson (A.I Agent) on behalf of Agno JF

**Status:** Ready for local feature development. This environment is not staging or production.

## Location

| Item | Value |
|---|---|
| Host | Local macOS development machine |
| Application source | `projects/lead-lab/application/source/` |
| Local URL | `http://127.0.0.1:8000` |
| Database | SQLite at `database/database.sqlite` |
| File storage | Laravel local disk under `storage/app/private/` |
| UI | Inertia React, TypeScript, Tailwind CSS v4, shadcn/ui Radix base |

## Toolchain

| Tool | Version or state |
|---|---|
| PHP | 8.5.9 |
| Composer | 2.10.2 |
| Laravel | 13.26.1 |
| Node.js | 26.5.0 |
| npm | 11.17.0 |
| Vite | 8.2.1 |
| shadcn CLI | 4.18.0 |
| Package manager | npm |

## Start Commands

Run these from `projects/lead-lab/application/source/`:

```text
php artisan serve --host=127.0.0.1 --port=8000
npm run dev
```

Use `npm run build` for a production asset build. Use `php artisan test`, `npm run lint:check`, `npm run format:check`, `npm run types:check`, and `composer run lint:check` for the local quality checks.

## Current Validation

- Laravel application scaffold completed.
- shadcn components are installed in `resources/js/components/ui/`.
- Lead Lab dashboard shell, session publishing, protected resources, and member access controls are implemented.
- TypeScript, ESLint, Prettier, Vite build, PHPUnit, and PHP Pint checks pass.

## Limitations

- SQLite is a local development choice. Staging must verify MySQL or MariaDB compatibility.
- HTTPS, backups, restore testing, monitoring, staging deployment, and IT ownership are not provided by this local environment.
- The local vertical slice uses synthetic seeded accounts and content. It is not staging or production evidence.
