# Workspace Initialization Record
**AI-generated by:** Alson (A.I Agent)

Next action: use `workspace/` for normal builds and invoke `workspace/workflows/matic/CONTEXT.md` when an open issue has the `matic` label.

## Destination

| Field | Decision |
|---|---|
| Workspace | `workspace/` |
| Configuration type | Extend the existing build workspace |
| Date | 2026-09-13 |
| Source repository | `adventistasia/lead-lab` |
| Matic trigger | Invocation-driven processing of open issues labeled `matic` |
| GitHub label | `matic` created with description `Authorize invocation-driven Alson issue workflow` |
| Background automation | None configured |

## Confirmed Behavior

- The existing Define, Design, Build, Measure, and Learn stages are preserved.
- Matic selection ranks eligible issues by change surface, dependency and unknown burden, verification burden, and risk or reversibility. Lower total is simpler. Issue number breaks ties.
- Applying `matic` authorizes the implementation workflow, PM Control updates, branch work, commit, push, and PR creation. Human-only product, privacy, provider, production, release, merge, and irreversible decisions remain gated.
- The plan-control stage updates relevant PM Control records before implementation.
- Reconciliation updates all affected carriers before Measure. It does not rewrite unrelated files or close controls early.
- Measure requires an 80% or higher quality score and `Pass` for every applicable acceptance criterion.
- Matic PRs are created from `origin/staging`, target `staging`, request Dennis Arquillano (`dennisatssd`), and do not use an issue auto-close keyword.
- The close stage closes the issue only after the exact recorded PR is verified merged into `staging`.
- Waiting for review or merge is resumable. Review changes route back to the smallest responsible stage.

## Source Of Truth Decisions

| Area | Source |
|---|---|
| Workspace behavior and routing | `workspace/AGENTS.md` and `workspace/CONTEXT.md` |
| Quality and recovery | `workspace/_config/quality-policy.md` and `workspace/_config/run-state-template.md` |
| Application code and tests | `application/source/` and `application/tests/` |
| Project controls | `pm-control/registers/` |
| Issue and PR state | GitHub repository `adventistasia/lead-lab` |
| Per-run evidence | `application/runs/` |

## Explicit Non-Goals

- No existing issue receives the `matic` label automatically.
- No GitHub Action or unattended agent runner is added.
- The existing application build stages are not replaced.
- No production deployment or production data operation is authorized.
- No external bot-verification provider, mail provider, privacy arrangement, or launch decision is selected by this setup.

## Validation Result

The workspace validation passed on 2026-09-13:

- Existing Define, Design, Build, Measure, and Learn stages remain present.
- Matic stages declare Inputs -> One Transformation -> Outputs, input gates, outputs, and verification checks.
- Modified workspace contracts contain no obsolete `projects/lead-lab/`, absent `_core/`, or stale repository-identity path.
- Required project source, application source, environment, and PM Control locations exist.
- All modified Markdown artifacts include the required Alson attribution and next action.
- `git diff --check` passed.
- The GitHub `matic` label was verified with its configured description.
