# PM Control Reconciliation: Issue #34 Production APP_URL
**AI-generated by:** Alson (A.I Agent)

The approved APP_URL-only scope was applied to the branch-controlled production Compose definition, repository guidance, and PM Control records. The private production environment could not be deployed or verified because its deployment `.env` and server access were unavailable. GitHub issue #34 (Live site not showing) was not updated.

## Applied Changes

| Control item | Result | Evidence |
|---|---|---|
| D-71 (Should this work change only the production `APP_URL` while the resident developer handles the remaining site-access fix?) | Made. The scope is limited to the branch-controlled production `APP_URL` deployment value and related guidance. | `pm-control/registers/decision-log.md`; current conversation, 2026-09-10 |
| CHG-48 (Set the production `APP_URL` to the canonical HTTPS address) | Approved. The production Compose definition and repository guidance now supply `APP_URL=https://leadhub.adventist.asia`; application code and network configuration are excluded. | `application/deploy/compose.yaml`; `application/deploy/.env.example`; `application/deploy/README.md`; `pm-control/registers/change-log.md` |
| ACT-68 (Set and verify the production `APP_URL`) | In Progress. The branch value is ready for deployment, but container verification remains pending operator access. | `application/deploy/compose.yaml`; `pm-control/registers/action-log.md`; `pm-control/registers/issues-log.md` |
| ISS-16 (Live site does not fully load because HTTPS asset requests and SSD-network access remain unresolved) | In Progress. The issue remains open until live browser and network checks pass. | `pm-control/registers/issues-log.md`; GitHub issue #34 (Live site not showing), `https://github.com/adventistasia/lead-lab/issues/34` |
| DEP-05 (Domain name availability and production URL) | Open. The canonical target is recorded, but deployment, DNS, TLS, and SSD-network evidence remain open. | `pm-control/registers/dependencies-log.md`; `application/environment/gate-1-environment-and-ownership.md` |

## Verification

| Check | Result |
|---|---|
| Authority | Pass. The user explicitly authorized the plan and explicitly excluded the GitHub update. |
| Scope | Pass. Only the APP_URL-related deployment configuration, repository guidance, environment evidence, and PM Control records were changed. |
| Secret safety | Pass. No private credentials or local runtime secrets were added or changed. |
| Production deployment | Not run. The private deployment `.env` and server access were unavailable. |
| Container recreation | Not run. No production containers were changed. |
| GitHub issue update | Not executed, per explicit instruction. |
| Acceptance | Open. ACT-68 (Set and verify the production `APP_URL`) and ISS-16 (Live site does not fully load because HTTPS asset requests and SSD-network access remain unresolved) remain In Progress. |

## Next Trigger

When this branch is deployed, recreate `app`, `queue`, and `scheduler`, verify Laravel's cached URL and each role's environment value, and attach the evidence to ACT-68 (Set and verify the production `APP_URL`). The resident developer then continues the excluded proxy, DNS, TLS, firewall, and SSD-network work. Do not update or close GitHub issue #34 (Live site not showing) from this run.
