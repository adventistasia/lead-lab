# PM Control Session Reconciliation
**AI-generated by:** Alson (A.I Agent)

**Result:** The PM-confirmed changes from the 2026-09-15 session are reconciled across the current PM Control records. Lead Hub is recorded as successfully launched. Prior register entries remain preserved as history; the latest status notes in the canonical records and this reconciliation define the current disposition.

## Actions

Closed after launch and PM direction:

- ACT-71 (Implement and verify the password policy change from issue #36 (Security: Lower Password Requirements))
- ACT-69 (Implement and verify LEADHub email branding across notification output and runtime configuration)
- ACT-68 (Set and verify the production `APP_URL`)
- ACT-66 (Implement and verify administrator account-state tabs and pending counts)
- ACT-67 (Implement and verify the registration-pending support contact)
- ACT-65 (Implement and verify the administrator guidelines page and navigation)
- ACT-63 (Implement and verify the session detail mobile overflow correction)
- ACT-61 (Implement and verify administrator batch upload for session supporting materials)
- ACT-58 (Implement and verify supporting-material file type expansion and the 25 MB limit)
- ACT-21 (Restore and verify email verification and real mail transport before staging or participant use)

ACT-48 (Implement and verify the mobile fullscreen toggle across authenticated application shell variants) is Closed by PM direction because work will not continue. This is an administrative closure and does not claim browser, responsive, or staging verification.

## Issues

Closed after launch and PM direction:

- ISS-18 (Password-policy change from issue #36 (Security: Lower Password Requirements) is being implemented under approved controls)
- ISS-16 (Live site does not fully load because HTTPS assets and SSD-network access remain unresolved)
- ISS-14 (Session detail page is not responsive on mobile when a long uploaded session-material filename is present)
- ISS-04 (Participant email data handling and privacy rules not confirmed for the Option B application)

ISS-10 (Administrative activity logging is incomplete in the Option B application) remains In Progress.

## Risks

Closed after launch and PM direction:

- R-19 (Lowering password requirements may increase weak-password compromise risk or conflict with security acceptance)
- R-04 (Participant email data handling unclear; privacy rules not confirmed)
- R-03 (Option B authentication or access control may expose participant content)
- R-15 (Calendar reminder delivery may be late, duplicated, or unavailable)

R-18 (Automated bots may abuse public registration and forgot-password endpoints) is In Progress by PM direction. The risk register lifecycle retains it as an active risk until the controls and verification are complete.

## Dependencies

Closed after launch and PM direction:

- DEP-05 (Domain name availability and production URL)
- DEP-09 (Option B authentication and participant-data handling decision)
- DEP-11 (Production email transport, queue worker, and scheduler for calendar reminders)
- DEP-12 (Staging and production email delivery operations for registration alerts)
- DEP-13 (Password-recovery email delivery and operational support)

DEP-10 (Gate 1 environment and ownership evidence for credible staging and production planning) remains Open.

## Decisions

- D-27 (What working participant-data rules should supplement D-23?) is Made. Participant name, email, and access status may be stored; the approved participant list remains outside the application; revoked records follow the three-month retention rule; and administrators only may view or change participant data.
- D-50 (Should Lead Hub use Amazon SES for production email and approve the required AWS account, region, external-processor, and data-processing arrangements?) is Made. Postmark SMTP is used in production, matching staging; Amazon SES is not selected.
- D-53 (Who is on call for deployment operations, and to which channels and severity levels should monitoring alerts be routed?) is Made for alerts received by Agno JF, Carmen, and Stephen. Severity and escalation handling remain undefined.
- D-49 (Will the `ssd.org` and `adventist.asia` domain administrators approve Cloudflare Tunnel CNAMEs for Lead Lab?), D-51 (Which production queue driver and worker topology should Lead Lab use: a database queue on a dedicated `queue.sqlite` file with one worker, or Redis with two workers?), D-52 (Should internal administrator registration alerts remain inside the Microsoft 365 trust boundary through Direct Send or Graph, or may all application email use SES?), and D-55 (Which offsite backup destination should Lead Lab use: M365 SharePoint or a paid B2 or R2 bucket?) remain Proposed pending reconciliation with current production operations.

## Current Attention

- ISS-10 (Administrative activity logging is incomplete in the Option B application) remains In Progress.
- R-18 (Automated bots may abuse public registration and forgot-password endpoints) remains In Progress.
- DEP-10 (Gate 1 environment and ownership evidence for credible staging and production planning) remains Open.
- R-08 (Application or server security compromised because patches are not applied), R-09 (Backups not configured; content loss on server failure), and R-10 (Server availability not guaranteed; portal offline) remain Open.
- D-49 (Will the `ssd.org` and `adventist.asia` domain administrators approve Cloudflare Tunnel CNAMEs for Lead Lab?), D-51 (Which production queue driver and worker topology should Lead Lab use: a database queue on a dedicated `queue.sqlite` file with one worker, or Redis with two workers?), D-52 (Should internal administrator registration alerts remain inside the Microsoft 365 trust boundary through Direct Send or Graph, or may all application email use SES?), and D-55 (Which offsite backup destination should Lead Lab use: M365 SharePoint or a paid B2 or R2 bucket?) need reconciliation against the running production configuration.
- D-53 (Who is on call for deployment operations, and to which channels and severity levels should monitoring alerts be routed?) needs severity and escalation rules if those are to be operationalized.

**Process condition:** PM-07 (Monitor and Control Project Performance) remains Attention. No project scope or schedule baseline change is recorded by this reconciliation.
